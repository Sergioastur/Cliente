// La aplicación permitirá dar de alta, baja, modificar y consultar piezas de carpintería. 
function alta () {
    function errorChk() {
        let hoy = Date.now();
        let fecha = Date.parse($('#color').val());
        if (fecha != hoy) {
            
        }
    }


    form.addEventListener("change", errorChk, false);



}